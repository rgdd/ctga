control ctgaVerifyChecksum(inout header_t h, inout metadata_t m) {
  apply {
    // intentionally left empty
  }
}

control ctgaComputeChecksum(inout header_t h, inout metadata_t m) {
  apply {
    // intentionally left empty
  }
}
